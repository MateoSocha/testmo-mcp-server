version: 2
updates:
  - package-ecosystem: npm
    directory: /
    schedule:
      interval: weekly
    labels:
      - dependencies
    ignore:
      # Major version bumps may have breaking changes — upgrade manually.
      - dependency-name: "*"
        update-types: ["version-update:semver-major"]
